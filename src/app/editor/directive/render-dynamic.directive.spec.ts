import { RenderDynamicDirective } from './render-dynamic.directive';

describe('RenderDynamicDirective', () => {
  it('should create an instance', () => {
    const directive = new RenderDynamicDirective();
    expect(directive).toBeTruthy();
  });
});
